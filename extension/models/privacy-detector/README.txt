Put the trained YOLOX-nano ONNX model here as model.onnx. Keep classes.json synchronized with the training class order.
The detector is advisory; a missing or failed detector never permits automatic image upload.
